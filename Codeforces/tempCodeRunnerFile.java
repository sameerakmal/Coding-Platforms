for (int i = 2; i < n; i+=2) {
            //     if(arr[1] == arr[i]) idx = i;
            // }
            // System.out.println(idx);
            // int s = Math.abs(arr[0] - arr[idx-1]);
            // System.out.println(s * s);